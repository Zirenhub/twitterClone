import { collection, onSnapshot, query, where } from 'firebase/firestore';
import { db } from '../../Firebase';

const getMessages = async (profileID, userID, setChatMessages) => {
  if (profileID && userID) {
  }
};

export default getMessages;
